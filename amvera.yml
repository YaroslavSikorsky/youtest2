

  meta:
    environment: jvm
    toolchain:
      name: maven
      version: "17"
  build:
    image: null
    args: null
    artifacts:
      target/*.jar: /
  run:
    image: null
    jarName: main.jar
    persistenceMount: /data
    containerPort: "80"
    servicePort: "80"
